Files in this directory are served only through `/api/resume/download`, after a visitor
verifies their email with a one-time PIN. They are intentionally kept outside `public/`
so they can't be downloaded by guessing a static URL.

Add your resume here as:

```
private/resume.pdf
```
